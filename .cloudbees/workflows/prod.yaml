apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:

jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: GoRepoWithFewerFindings
          artifact-id: b3b09eb6-bf91-4384-b853-f731f06d5a5c
          target-environment: ASPM_QA_ENV